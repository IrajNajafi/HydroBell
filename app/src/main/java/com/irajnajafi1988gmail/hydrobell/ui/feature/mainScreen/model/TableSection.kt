package com.irajnajafi1988gmail.hydrobell.ui.feature.mainScreen.model

import com.irajnajafi1988gmail.hydrobell.R

enum class TableSection(val label : String,val icon:Int) {
    HOME(label = "Home", icon = R.drawable.drop),
    HISTORY(label = "History", icon = R.drawable.history),
    SETTING(label = "Settings", icon = R.drawable.settings)
}